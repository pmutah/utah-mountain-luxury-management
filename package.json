{
  "name": "utah-mountain-luxury-management",
  "version": "1.0.0",
  "private": true,
  "description": "Utah Mountain Luxury Management — short-term rental operations board",
  "workspaces": [
    "api",
    "web"
  ],
  "scripts": {
    "dev": "npm run dev -w web",
    "dev:api": "concurrently -n api,web -c blue,green \"npm run start:dev -w api\" \"npm run dev -w web\"",
    "build": "npm run build -w web && npm run build -w api",
    "start": "npm run start:prod -w api",
    "seed": "npm run seed -w api",
    "lint": "npm run lint -w api && npm run lint -w web"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
