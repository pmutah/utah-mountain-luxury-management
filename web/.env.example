# Leave unset so localhost:5173 uses the hosted Cloudflare Pages APIs
# (same calendar, bills, and construction data as https://wilhite-portfolio.pages.dev).
# Only set this if you intentionally want a different backend.
# VITE_API_URL=